# 🌱 디지털 가드닝 웹앱 기획문서

> **프로젝트 유형** : 웹퍼블리셔 서브프로젝트 (포트폴리오 겸 학습 프로젝트)
> **작성일** : 2026-07-13
> **참고 서비스** : [Plant Diary](https://plantdiaryapp.com/)

---

## 1. 프로젝트 개요

### 1.1 목적
- 식물 관리(물주기·성장 기록)를 사진 + 버튼 클릭 + 짧은 메모로 최소 마찰 기록
- React 생태계 실무 전환 학습 (Vue → React)
- 접근성(A11y) 역량을 React 컴포넌트 설계 단계부터 적용한 포트폴리오 확보

### 1.2 작업자 프로필 및 학습 목표
| 항목 | 내용 |
|---|---|
| 경력 | 퍼블리셔 10년차 |
| 보유 역량 | Vue 실무 경험, 웹접근성 인증마크(WA) 획득 경험, HTML/SCSS/JS |
| 신규 학습 | **React 기반 프레임워크 첫 프로젝트** |
| 학습 전략 | Vue 개념 → React 개념 매핑표 활용 (§7.1 참조) |

### 1.3 기술 스택
| 구분 | 선정 | 비고 |
|---|---|---|
| 프레임워크 | **React 18 + Vite** (권장) 또는 Next.js | 첫 React라면 Vite 권장 — SSR/라우팅 개념 부담 없이 컴포넌트에 집중. PWA 확장 시에도 vite-plugin-pwa로 충분 |
| 스타일 | SCSS (CSS Modules 방식 `*.module.scss`) | Vue의 scoped style과 가장 유사한 경험 |
| 상태관리 | useState/useContext → 필요시 Zustand | Redux는 첫 프로젝트에 과함. Zustand는 Vue의 Pinia와 개념 유사 |
| 데이터 저장 | **IndexedDB (Dexie.js)** | 서버 없이 로컬 저장. PWA 오프라인 요구와 자연 연결 |
| 컴포넌트 문서화 | Storybook 8 | 컴포넌트 단위 개발(CDD) + 접근성 애드온(a11y addon) 활용 |
| 작업관리 | Notion (칸반 + 스프린트 DB) | |
| 디자인 | Figma + **Figma MCP 연동** | 디자인 토큰(색상/타이포/간격) → SCSS 변수 자동 추출 |

---

## 2. 참고 서비스 분석 — Plant Diary

| 기능 | Plant Diary | 본 프로젝트 반영 |
|---|---|---|
| 물주기 리마인더 (반복 스케줄) | ✅ 핵심 | ⭕ 축소 반영 — "물 준 날짜 기록" + D+n 경과 표시 |
| 성장 사진 일지 | ✅ | ⭕ 메인 기능 |
| 건강 상태/증상 기록 | ✅ | 🔺 후순위 (텍스트 기록으로 대체 가능) |
| 식물 컬렉션 검색 | ✅ | 🔺 후순위 (식물 수 적을 때 불필요) |
| 관리 캘린더 | ✅ | ⭕ **추가 권장** (§4.1) |
| PWA (모바일 앱 유사 경험) | ✅ meta 태그 확인됨 | ⭕ 서브기능 4번과 일치 |

**시사점** : Plant Diary의 핵심 UX는 "오늘 뭘 해야 하는지 한눈에 보이는 케어 큐(care queue)". 단순 기록 앱이 아니라 **'오늘 할 일' 중심 화면 구성**이 차별점이므로 홈 화면 설계에 반영.

---

## 3. 기능 정의

### 3.1 메인 기능 (MVP — 1차 목표)

#### F1. 식물 등록 및 사진 업로드
- 식물 프로필 생성 : 이름(별명), 종류, 대표 사진, 등록일
- 성장 사진 타임라인 : 날짜별 사진 누적 → 세로 타임라인 UI
- 이미지 처리 : 업로드 시 클라이언트 리사이징(Canvas API, 최대 1200px) → IndexedDB 용량 관리
- **A11y** : 사진 업로드 시 대체텍스트 입력 필드 필수 노출 (§4.4 자동화와 연결)

#### F2. 물주기 원클릭 기록
- 식물 카드의 💧 버튼 클릭 → 오늘 날짜로 즉시 기록 (확인 모달 없이, 실행취소 토스트 제공)
- 카드에 "마지막 물 준 날 D+3" 형태 경과 표시
- 경과일 기준 시각 상태 : 🟢 정상 / 🟡 주의 / 🔴 오래됨 (식물별 물주기 주기 설정값 기준)
- **A11y** : 버튼에 `aria-label="몬스테라에 물주기 기록, 마지막 물주기 3일 전"` 식 동적 레이블

#### F3. 간단 텍스트 기록 (일지)
- 사진 또는 물주기 기록에 한 줄 메모 첨부
- 독립 메모도 가능 ("새 잎 나옴", "잎끝 갈변")
- 이모지 태그 3~5종 (🌿새잎 / 🐛병충해 / 🌸개화 / ✂️가지치기) — 나중에 필터/그래프 재료가 됨

### 3.2 서브 기능 (2차 — 우선순위 순)

| 순위 | 기능 | 내용 | 학습 포인트 |
|---|---|---|---|
| S1 | **A11y 심화** | 키보드 온리 전체 플로우, 스크린리더 검증, 대체텍스트 보조 | 기존 강점의 React 적용 — **가장 먼저** |
| S2 | **PWA 오프라인** | Service Worker + IndexedDB, 설치 가능 앱 | vite-plugin-pwa |
| S3 | **인터랙션** | Framer Motion — 식물 성장 애니메이션, 페이지 전환 | `motion`, `AnimatePresence`, `layoutId` |
| S4 | **데이터 시각화** | Recharts — 물주기 주기 그래프, 기록 히트맵 | D3는 러닝커브 높음, Recharts 우선 |

> ⚠️ **우선순위 조정 제안** : 원안은 인터랙션→시각화→A11y→PWA 순이었으나, A11y는 나중에 얹는 게 아니라 **컴포넌트 설계 단계부터 반영해야 재작업이 없음**. 인증마크 경험자의 강점이 살아나는 지점이기도 함. PWA는 오프라인 데이터 구조(IndexedDB)를 MVP부터 쓰면 추가 비용이 작아서 2순위로 상향.

---

## 4. 보강 제안 (추가 권장 기능)

### 4.1 ⭐ 관리 캘린더 뷰 (강력 권장)
- 월간 캘린더에 물주기/기록 도트 표시 — Plant Diary의 핵심 가치이며, 데이터가 쌓이는 재미 = 리텐션의 핵심
- 구현 난이도 낮음 (CSS Grid 7열), 시각화(S4)의 기초 데이터 구조와 공유
- **A11y 포인트** : `role="grid"` 캘린더의 키보드 내비게이션(화살표 키 이동)은 접근성 포트폴리오에서 임팩트 큰 패턴

### 4.2 ⭐ 물주기 주기 설정 + 알림 배지
- 식물별 "며칠마다 물 주기" 설정 → 홈에서 "오늘 물 줄 식물 2개" 요약 배지
- 푸시 알림까지는 불필요(서버 필요). **앱 내 배지 + PWA 뱃지 API**로 충분
- Plant Diary의 "care queue" 개념을 최소 비용으로 구현

### 4.3 사진 비교 슬라이더 (Before/After)
- 첫 사진 vs 최신 사진 좌우 슬라이더 비교 — "식물이 자랐다"는 정서적 보상 극대화
- Framer Motion 드래그 제스처 학습과 자연 연결 (S3에 편입 가능)

### 4.4 대체텍스트 자동화 구체화
- 원안의 "이미지 대체텍스트 자동화"는 범위가 모호하므로 3단계로 구체화 :
  1. **1단계(MVP)** : 템플릿 자동 생성 — `"{식물이름} {날짜} 성장 사진{, 메모: ...}"` 형태로 기본값 채움 + 수정 가능
  2. **2단계** : 이모지 태그 반영 — "새 잎이 난 몬스테라"
  3. **3단계(선택)** : Claude API 연동으로 사진 설명 자동 생성 (외부 API 의존이므로 실험 기능으로 분리)

### 4.5 데이터 백업/복원 (JSON 내보내기·가져오기)
- 로컬 저장(IndexedDB) 특성상 브라우저 데이터 삭제 = 전체 유실
- JSON 파일 내보내기/가져오기 버튼은 반나절 작업으로 신뢰성 확보 — **MVP에 포함 권장**

### 4.6 다크모드
- SCSS 변수 + `prefers-color-scheme` — Figma 디자인 토큰 워크플로 검증에 좋은 소재
- 명도 대비(WCAG 1.4.3) 검증을 두 테마에서 수행 → 접근성 포트폴리오 보강

---

## 5. 화면 구성 (IA)

```
홈 (오늘의 케어 큐)
├─ 오늘 물 줄 식물 요약 배지
├─ 식물 카드 그리드 (사진 + D+n + 💧버튼)
└─ + 식물 추가

식물 상세
├─ 프로필 (대표사진, 물주기 주기 설정)
├─ 성장 타임라인 (사진 + 메모, 무한스크롤)
├─ 사진 비교 슬라이더
└─ 그래프 탭 (S4)

캘린더 (월간 기록 뷰)

설정
├─ 데이터 백업/복원
├─ 테마 (라이트/다크)
└─ 접근성 옵션 (모션 감소 등)
```

---

## 6. 접근성 요구사항 (WCAG 2.2 AA 기준)

| 영역 | 요구사항 |
|---|---|
| 키보드 | 모든 기능 키보드 조작 가능, 포커스 트랩(모달), Skip link, 캘린더 화살표 키 내비게이션 |
| 스크린리더 | 물주기 기록 완료 시 `aria-live="polite"` 알림, 동적 aria-label, 랜드마크 구조 |
| 이미지 | 모든 사진 대체텍스트 필수 (§4.4 자동화) |
| 색상 | 상태 표시(🟢🟡🔴)는 색+아이콘+텍스트 병행, 명도 대비 4.5:1 이상 |
| 모션 | `prefers-reduced-motion` 대응 — Framer Motion `useReducedMotion` 훅 필수 적용 |
| 검증 | Storybook a11y addon(axe-core) CI 수준 상시 검증 + NVDA/VoiceOver 수동 테스트 |

---

## 7. React 전환 가이드 (Vue 경험자용)

### 7.1 개념 매핑표
| Vue | React | 비고 |
|---|---|---|
| `data()` / `ref()` | `useState` | |
| `computed` | `useMemo` (또는 그냥 렌더 내 계산) | React는 대부분 그냥 변수로 충분 |
| `watch` | `useEffect` | 남용 주의 — React 첫 프로젝트 최대 함정 |
| `v-if` / `v-for` | `{cond && <X/>}` / `arr.map()` | JSX는 "그냥 JS" |
| `v-model` | `value` + `onChange` 수동 바인딩 | |
| `props` / `emit` | `props` / 콜백 props 전달 | |
| slot | `children` / render props | |
| scoped style | CSS Modules (`*.module.scss`) | |
| Pinia | Zustand | API 감각 거의 동일 |

### 7.2 주의 포인트
- **useEffect 남용 금지** : Vue의 watch 감각으로 useEffect를 쓰면 안티패턴. "파생 값은 렌더 중 계산"이 기본
- 불변성 : 상태 직접 변경 금지 (`arr.push` ❌ → `[...arr, item]`)
- 리렌더 모델 이해 : Vue의 반응성 추적과 달리 React는 컴포넌트 함수 전체가 다시 실행됨

---

## 8. 작업 프로세스

### 8.1 워크플로
```
Figma (MCP 연동)
  └→ 디자인 토큰 추출 → _tokens.scss
       └→ Storybook 컴포넌트 개발 (CDD + a11y addon)
            └→ 페이지 조립 → 기능 구현
                 └→ Notion 태스크 상태 갱신
```

### 8.2 단계별 마일스톤 (제안)
| 단계 | 기간(안) | 산출물 |
|---|---|---|
| M0 환경구축 | 1주 | Vite+React+SCSS+Storybook 세팅, Figma 토큰 파이프라인, React 개념 학습 |
| M1 디자인시스템 | 1~2주 | 버튼/카드/입력/모달 등 기초 컴포넌트 (Storybook, a11y 통과) |
| M2 MVP | 2~3주 | F1~F3 + 백업/복원(4.5), IndexedDB 저장 |
| M3 A11y 심화 + 캘린더 | 1~2주 | 키보드 온리 검증, 스크린리더 테스트, 캘린더 뷰(4.1) |
| M4 PWA | 1주 | 오프라인 동작, 설치 배너, 케어 배지(4.2) |
| M5 인터랙션/시각화 | 2주+ | Framer Motion, Recharts, 비교 슬라이더(4.3) |

### 8.3 Notion 관리 구조 (제안)
- **태스크 DB** : 상태(백로그/진행/리뷰/완료) × 마일스톤(M0~M5) × 유형(퍼블리싱/기능/A11y/학습)
- **학습 노트 DB** : React 개념별 Vue 대응 정리 (매핑표 확장)
- **A11y 체크리스트 DB** : WCAG 항목별 통과 여부 — 인증마크 심사 경험 포맷 재활용

---

## 9. 리스크 및 대응
| 리스크 | 대응 |
|---|---|
| React 러닝커브로 일정 지연 | M0에 학습 주간 명시 확보, Next.js 대신 Vite로 범위 축소 |
| IndexedDB 사진 용량 초과 | 업로드 시 리사이징 필수, 저장 용량 표시 UI |
| 로컬 데이터 유실 | JSON 백업/복원 MVP 포함 (§4.5) |
| D3 러닝커브 | Recharts 우선, D3는 필요 시 특정 차트만 부분 도입 |
| 애니메이션 접근성 충돌 | `useReducedMotion` 전역 적용 원칙 수립 |

---

## 10. 요약 — 원안 대비 변경/보강 포인트

1. **서브기능 우선순위 변경** : A11y → PWA → 인터랙션 → 시각화 (A11y는 설계 단계부터, PWA는 데이터 구조와 동시 결정 필요)
2. **추가 필수 기능** : ⭐관리 캘린더 뷰, ⭐물주기 주기 설정+케어 배지, 데이터 백업/복원
3. **추가 권장 기능** : 사진 비교 슬라이더, 다크모드
4. **대체텍스트 자동화 3단계 구체화** : 템플릿 → 태그 반영 → AI 생성(실험)
5. **기술 선택 가이드** : Vite 권장(첫 React), Zustand(Pinia 유사), Recharts 우선(D3 후순위), Dexie.js(IndexedDB)
